let isValid = true;

// sem ternario

function verify (isValid) {
    if (!isValid){
    return true;
    } else { 
        return false;
    }
}

// com ternarios

const result = isValid ? true : false;

let zero = 0;
const meuResultado = zero > 0 ? 0 : -1;

